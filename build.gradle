group = 'com.pi'
version = '0.0.1'

ext {
    db = {}
    db.username = "pi_kursy"
    db.password = "pi_kursy"
    db.name = "pi_kursy"
    db.host = "localhost"
    db.port = "5432"
    db.schema = "pi_kursy"
    db.url = "jdbc:postgresql://${db.host}:${db.port}/${db.name}"
}